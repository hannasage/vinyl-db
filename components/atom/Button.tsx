import React from 'react';
import { MatrixBoxBaseProps } from '@/components/MatrixBox';

export interface ButtonBaseProps {}

export const Button = ({ size, className }: MatrixBoxBaseProps & ButtonBaseProps) => {
  return(
    <button>

    </button>
  )
}
